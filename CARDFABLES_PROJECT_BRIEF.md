# CardFables — Project Brief

> Pass this document to Claude Code or Cowork to get full context on what we're building, why, and how.

---

## What is CardFables?

CardFables is a white-label **image-to-story SaaS pipeline**. The owner uploads 1–3 card/product images through a simple app, Claude analyzes the images and generates dual-age narrative content, and the result is automatically published to a live website via GitHub + Vercel.

The proof-of-concept is a Pokémon card soap opera called **"Flames of Our Lives"** — each card becomes a new episode in a serialized story. But the engine is generic and can be repackaged for any industry (e-commerce, real estate, museums, marketing agencies, etc.).

---

## Business Model

- Multi-tenant SaaS: one codebase, separate content per client
- Each client gets their own subdomain and config folder
- Clients bring their own Anthropic API key (or optionally pay through the platform)
- Pricing tiers:

| Tier | Price | Episodes included |
|------|-------|-------------------|
| Starter | $29/mo | 25 |
| Growth | $79/mo | 100 |
| Studio | $199/mo | 500 |

- API cost per episode: ~$0.034 (Claude Sonnet 4.6)
- Gross margin: ~91–97%

---

## Tech Stack

- **Language:** Node.js
- **AI:** Anthropic Claude API (claude-sonnet-4-6) — vision + story generation
- **Frontend:** React / Next.js (existing CardFables JSX prototype)
- **Hosting:** Vercel (auto-deploys on GitHub push)
- **Repo:** GitHub account `siulm` (repo: `cardfables`)
- **Shipping/deploy automation:** Filesystem MCP + GitHub MCP (via Claude Code)
- **Target for v1:** Local only (Mac), Docker-ready for future packaging

---

## Core Pipeline (what happens per episode)

```
Owner uploads 1–3 card images
        ↓
Node.js backend reads story-bible.json (existing characters, plot, last episode)
        ↓
Claude API call:
  - Vision analysis: reads card art, scene, mood, Pokémon name
  - Story generation: writes Junior Fables (ages 6–11) + Full Fables (ages 12+)
  - Context manager: determines what to update in story bible
        ↓
Structured JSON output:
  {
    episode: N,
    cards: ["Card Name"],
    title: "Episode title",
    junior: "...",
    full: "...",
    cliffhanger: "..."
  }
        ↓
Filesystem MCP writes episode-N.json to project
        ↓
story-bible.json updated with new characters/plot state
        ↓
GitHub MCP commits + pushes to siulm/cardfables
        ↓
Vercel auto-deploys → live website updated ✅
```

---

## Multi-Tenant Structure

One repo, one Vercel deployment, many clients:

```
/clients/
  /pokemon-fables/          ← CardFables (client #1, proof of concept)
    config.json
    story-bible.json
    episodes/
      episode-1.json
      episode-2.json
  /acme-stories/            ← future client
    config.json
    ...
/engine/                    ← shared website code (Next.js)
```

Each client gets a subdomain: `pokemon-fables.yourdomain.com`

---

## Client Config File (per client)

```json
{
  "brand": "Flames of Our Lives",
  "tone": "soap opera, dramatic, funny, age-appropriate",
  "audience_junior": "ages 6-11",
  "audience_full": "ages 12+",
  "output_format": "dual-age episode",
  "anthropic_api_key": "client's own key",
  "amazon_affiliate_tag": "their-tag-20",
  "deployment": "vercel"
}
```

---

## Story Bible File (per client)

Persists story state across sessions. Read at start, updated at end of every episode.

```json
{
  "show_title": "Flames of Our Lives",
  "last_episode": 2,
  "characters": [
    { "name": "Charizard", "card": "Charizard V", "role": "reluctant hero, secretly kind" },
    { "name": "Venusaur", "card": "N/A", "role": "skeptical neighbor, growing to care" },
    { "name": "Mismagius", "card": "Mismagius ex", "role": "dramatic observer, reluctant hero" },
    { "name": "Genesect", "card": "Genesect V", "role": "antagonist, approaching from north" }
  ],
  "current_plot": "Genesect is heading toward the northern caves where Charizard secretly shelters baby Pokémon. Venusaur and Mismagius are racing to warn him.",
  "setting": "Verdant Valley",
  "running_themes": [
    "Charizard secretly helps everyone but is too proud to admit it",
    "Venusaur is judgmental but genuinely cares",
    "Mismagius observes and comments, resists heroism"
  ]
}
```

---

## CardFables Website (Live)

Full Next.js 16 app deployed on Vercel:

- Age toggle: 🐣 Junior Fables (6–11) / 🔥 Full Fables (12+)
- Multi-card sidebar (1–3 cards, each with Amazon buy button)
- Episode grid with "coming soon" locked episodes
- Episode 1 (Charizard V) and Episode 2 (Mismagius ex + Genesect V) hardcoded
- Episode 3 (Team Rocket's Dugtrio) generated by pipeline
- Global footer disclaimer (Pokémon IP + Amazon affiliate FTC)
- Per-episode one-liner: "An original fan story — not affiliated with The Pokémon Company"
- SEO: JSON-LD structured data, OpenGraph, sitemap, robots.txt
- Pending: refactor to read from episode JSON files instead of hardcoded data.ts

---

## Story Format

Each episode follows this structure:

**Full Fables (12+):**
- Scene directions in brackets: `[Scene opens on...]`
- Inner monologue and emotional depth
- Character dialogue with attribution dashes: `— Venusaur`
- Cliffhanger ending: `To be continued…`
- 600–900 words

**Junior Fables (6–11):**
- Same plot, same characters, same cliffhanger
- Short punchy sentences
- Sound effects: `CRUNCH. SNAP.`
- More action, less inner monologue
- 300–500 words

---

## What Has Been Built

### 1. Next.js website (complete)
Full Next.js 16 app with React 19, Tailwind CSS 4, deployed on Vercel:
- Episode reader with Junior/Full Fables age toggle
- Multi-card sidebar with affiliate product links
- Series listing, browse, shop, submit pages
- SEO: JSON-LD, OpenGraph, sitemap, robots.txt
- Currently reads from hardcoded `src/lib/data.ts`

### 2. Episode writer script (complete) — `scripts/generate-episode.js`
- Accepts: client name + image path(s) as args
- Reads: `clients/{client}/story-bible.json` + `clients/{client}/config.json`
- Calls: Claude Sonnet 4.6 API with vision (image analysis + story generation)
- Outputs: `clients/{client}/episodes/episode-N.json` (structured StoryBlock format)
- Updates: `story-bible.json` with new characters, plot, themes
- Cost: ~$0.03–0.05 per episode
- Usage: `node scripts/generate-episode.js pokemon-fables card1.jpg [card2.jpg] [card3.jpg]`

### 3. Client directory structure (complete)
```
clients/pokemon-fables/
  config.json          ← API key, brand, tone (gitignored)
  config.example.json  ← template without key (committed)
  story-bible.json     ← persistent story state
  series.json          ← series metadata: colors, taglines, "Coming Soon" series (pending)
  episodes/
    episode-1.json     ← exported from data.ts (pending)
    episode-2.json     ← exported from data.ts (pending)
    episode-3.json     ← first pipeline-generated episode
```

### 4. Tests (complete)
- 16 tests for utility functions (parseArgs, slugify, mergeBibleUpdates, buildSystemPrompt)
- Run: `node --test scripts/generate-episode.test.js`

---

## What Still Needs to Be Built (v1 — Local)

### 1. Website refactor — pre-build data pipeline (in progress)
The website reads episode content from hardcoded `src/lib/data.ts`. A pre-build script (`scripts/build-data.js`) will:
- Read episode JSON files from `clients/pokemon-fables/episodes/`
- Read series metadata from `clients/pokemon-fables/series.json` (colors, taglines, "Coming Soon" series)
- Auto-generate `src/lib/data.ts` with all data merged
- Run automatically before `next build` via `"prebuild"` in package.json

This means zero changes to components — they keep importing `SERIES` from `data.ts`.

New files needed:
- `scripts/build-data.js` — pre-build script
- `clients/pokemon-fables/series.json` — series metadata (non-story data: colors, taglines, etc.)
- `clients/pokemon-fables/episodes/episode-1.json` — exported from current data.ts
- `clients/pokemon-fables/episodes/episode-2.json` — exported from current data.ts

Full deploy flow after refactor:
```
Pipeline generates episode JSON → commit + push → Vercel runs prebuild → next build → live site updated
```

### 2. Deploy script (`scripts/deploy.js`)
- Commits episode JSON + updated story bible to git
- Pushes to GitHub
- Vercel auto-deploys → live website updated
- Logs result

### 3. Simple UI (optional v1 local app)
- Drag-and-drop card image(s)
- Hit "Generate Episode"
- Shows result preview
- One-click publish

---

## MCP Setup (Claude Code — local Mac)

Already configured in `claude_desktop_config.json`:
- **Filesystem MCP** — ships with Claude Code, reads/writes local project files
- **GitHub MCP** — official Anthropic GitHub MCP, commits + pushes to siulm/cardfables

Both MCPs are called by Claude Code orchestrating the pipeline — not by the Node.js app directly.

---

## API Cost Reference

| Model | Input | Output | Cost/episode |
|-------|-------|--------|--------------|
| Claude Sonnet 4.6 | $3/MTok | $15/MTok | ~$0.034 |
| Claude Haiku 4.5 | $1/MTok | $5/MTok | ~$0.011 |

Batch API gives 50% discount for non-realtime workloads.
Prompt caching (story bible + system prompt) saves ~60% on input costs.

---

## Future Roadmap

- Docker container packaging for on-premise client installs
- Web UI for non-technical clients (drag + drop → publish)
- Support for non-Pokémon verticals (swap config + system prompt)
- Client brings own Anthropic API key (zero API cost to platform)
- Analytics dashboard (episodes published, site traffic, affiliate clicks)

---

## Owner / Context

- Owner: Andrew (goes by Haruka in leotard business context)
- Based in Tokyo
- Also owns MASH Handpan (handpan retail, Shopify + Yamato) and MASH Leotard
- Prefers lean, low-labor operations
- Familiar with Claude Code, MCP setup, GitHub workflows
- Mac user (M2 Max), Claude Code installed with `--dangerously-skip-permissions`
- GitHub account `siulm`, repo `cardfables` (this project only — other repos are unrelated)
- Hosting on Vercel (connected to siulm/cardfables)

---

*Last updated: April 11, 2026. Episode generation pipeline complete. Website refactor (pre-build data pipeline) in progress.*
